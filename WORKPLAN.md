# תוכנית עבודה — שלב 4: מנוע האפקטים והוויזואל ("עיצוב פסיכי")

מסמך תכנון מקצה-לקצה. נכתב לפני שורת קוד אחת, על בסיס: 4 דוחות מחקר
(אנימציות גלילה וטקסט · אפקטי 3D ופרימיום · UX של בילדרים מובילים ·
ניתוח שני האתרים הקיימים של בעל המערכת — shosh-marketing ו-alon.ai).
תמציות המחקר המלאות שמורות בקבצי העבודה של הסשן (`research/*.md`).

---

## 1. מטרות (מה המשתמש ביקש)

1. **טקסט נגלל** — פס טקסט/לוגואים אינסופי (marquee) ובלוק "הצהרה" שבו
   הטקסט נדלק מילה-מילה תוך כדי גלילה.
2. **אפקטי 3D** — כרטיסים שמטים אחרי הסמן, הדמיית מוצר בפרספקטיבה בהירו,
   כפתורים תלת-ממדיים שנלחצים פיזית.
3. **כניסות ויציאות של טקסטים** — מנוע reveal עם כניסת מילה-מילה,
   טשטוש, זום; יציאת טקסט בגלילה החוצה.
4. **השקעה כבדה בעיצוב** — רקעים חיים (אורורה/נקודות/גריד/רעש-פילם),
   זוהר, ספוטלייט שעוקב אחרי הסמן, מסגרת-אור נעה, טקסט מנצנץ.
5. **כפתור איפוס תמיד** — בשלוש רמות: שדה בודד, בלוק, וגלובלי.
6. **החלפת פונטים** — חבילות פונטים מוכנות (כותרת+גוף כבחירה אחת) +
   התאמה ידנית.
7. **כל הזזת בלוק משנה את התצוגה בפועל** — מאומת ב-E2E ייעודי.
8. תהליך: תוכנית ← מימוש במודל sonnet ← סוכן QA שמוודא שכל סעיף קיים,
   נראה טוב ועובד מקצה לקצה.

## 2. עקרונות מחייבים (נלמדו מהמחקר — לא שבירים)

- **אפס ספריות אנימציה.** IntersectionObserver + CSS מכסים הכול;
  `animation-timeline: view()` רק כהעשרה בתוך `@supports` (פיירפוקס עדיין
  בלי תמיכה). זה גם מה שעלה מ-shosh: 90% מהאפקטים שם ידניים, ושתי
  הספריות שהותקנו שם היו מיותרות.
- **עוגת נסיגה בת 4 שכבות:** ‏(0) ה-HTML מהשרת תמיד גלוי — מצב "מוסתר"
  דורש גם `html[data-js]` (סקריפט inline לפני הידרציה) וגם
  `@media (prefers-reduced-motion: no-preference)`. ‏(1) כל CSS של תנועה
  בתוך המדיה-קוורי. ‏(2) IO reveals = סוס העבודה. ‏(3) אפקטי scrub רק
  ב-`@supports (animation-timeline: view())`, לעולם לא תוכן קריטי.
- **keyframes תמיד מ-מצב בסיס גלוי** — כך מתג ה-reduced-motion לא יכול
  להשאיר תוכן נעלם (באג אמיתי שנמצא ב-alon.ai ותוקן שם ידנית).
- **WCAG 2.2.2:** כל תנועה אוטומטית מעל 5 שניות (marquee, נצנוץ) מקבלת
  כפתור השהיה גלוי. hover-pause לבדו אינו מספיק.
- **RTL:** אפקטי ציר Y/שקיפות/טשטוש/סקייל — ניטרליים. כיוון אופקי —
  רק לוגי ("מהתחלה/מהסוף") דרך משתנה סימן; מתכון ה-marquee ל-RTL
  (track ‏ltr, פריטים rtl, אנימציה ל-translateX חיובי) מגיע ישירות
  מהקוד של shosh.
- **ביצועים:** לולאות מאנימות רק transform/opacity; אורורה בלי
  `filter: blur` (הרכות נאפית בגרדיאנט); ‏grain הוא data-URI סטטי;
  ‏glass רק כ-opt-in עם הנחתה במובייל; מאזין pointer אחד לסקשן,
  מצומצם ב-rAF.
- **תיקון חובה בקוד הקיים:** לבלוק ה-reduced-motion ב-`globals.css`
  חסר `animation-iteration-count: 1 !important` — בלעדיו אנימציה
  אינסופית ב-0.01ms מתרסקת לסופת ציור.
- **טעם המותג של הבעלים** (מנותח מהאתרים שלו): משקל כותרות 900,
  ‏easing חתימה `cubic-bezier(.2,.7,.2,1)` ו-`cubic-bezier(.22,1,.36,1)`,
  כניסות 0.7–1.1s, סטאגר ~0.09s, marquee 26–40s ליניארי, זוהר בצבע
  המבטא, גרדיאנטים מותג-אל-מותג ב-135°, בלובים דקורטיביים ב-8%–22%
  שקיפות.

## 3. ארכיטקטורה — איפה כל דבר גר

```
lib/effects.ts            ← חדש: טיפוסי אנימציה, פריסטים, מיפוי עוצמות, easing tokens
lib/theme.ts              ← הרחבת Theme.effects + פליטת משתני --ds-fx-* + normalizeTheme()
lib/rich-text.tsx         ← splitWords שמשתלב עם הדגשת ** ** הקיימת
app/globals.css           ← כל קלאסי .fx-* + CSS של reveal + תיקון reduced-motion
app/layout.tsx            ← סקריפט inline: document.documentElement.dataset.js=""
components/fx/            ← חדש: Reveal, TiltCard, SpotlightGroup, ThemeFxLayers,
                             CountUp, MarqueeTrack (פרימיטיבים בלבד)
components/theme-scope.tsx← חדש: עוטף .ds-scope שמוסיף data-fx-bg/card/motion + ThemeFxLayers
components/blocks/        ← אינטגרציה בבלוקים + 2 בלוקים חדשים: marquee, statement
components/studio/        ← טאב עיצוב מורחב, קבוצת אנימציה בעורך הבלוקים,
                             חבילות פונטים, איפוסים, replay, "ערבב לי"
themes/*.json             ← ברירות מחדל חדשות + ערכה שלישית "נובה פסיכית"
pages-data/demo.json      ← דף הדמו מציג את היכולות החדשות
```

**עיקרון הפרדה:** הערכה קובעת *אווירה* (רקע, סגנון כרטיסים, זוהר, עוצמת
אנימציה); הבלוק קובע *הדגשה* (אנימציה ספציפית, beam על מסלול מומלץ,
נצנוץ על כותרת). בלוקים לעולם לא מכירים את מנגנון האפקטים — הם עטופים בו.

### 3.1 סכמת הערכה החדשה (`Theme.effects`)

| מפתח | ערכים | ברירת מחדל |
|---|---|---|
| `shadow` | none / soft / strong (קיים; הצללות משודרגות ל-3 שכבות גוון) | soft |
| `background` | none / aurora / orbs / dots / grid | none |
| `backgroundIntensity` | 0–1 | 0.5 |
| `noise` | boolean (שכבת גרעון-פילם) | false |
| `cardStyle` | flat / raised / glass / tilt | raised |
| `glow` | none / soft / intense (ספוטלייט + הילות) | none |
| `buttonStyle` | flat / push (3D) / shine (ברק חולף) | flat |
| `animation.style` | none / fade / rise / scale / blur | rise |
| `animation.intensity` | subtle / dynamic / dramatic ("עדין/דינמי/פסיכי") | subtle |

מיפוי עוצמות: עדין = 12px / 500ms / סטאגר 60ms; דינמי = 24px / 600ms /
100ms; פסיכי = 40px + סקייל וטשטוש קלים / 800ms / 140ms (תקרה קשיחה —
בלי rotation-spam). ‏`normalizeTheme()` משלים את כל המפתחות לערכות ישנות
ולייבוא — אותו דפוס כמו `normalizePage()` הקיים.

### 3.2 סכמת אנימציה לבלוק (`content._anim`)

נשמר בתוך תוכן הבלוק (נשאר תואם לאחור — בלוק בלי `_anim` = "ירושה"):

```jsonc
{ "preset": "inherit" | "off" | "subtle" | "dynamic" | "dramatic",
  "type":   "inherit" | "fade" | "rise" | "slide-start" | "scale" | "blur" | "word-stagger",
  "durationMs": 600, "delayMs": 0, "staggerMs": 100, "once": true }
```

תקרות בסכמה: duration ≤ 2000, delay ≤ 1500, stagger ≤ 200. שדות
duration/delay/stagger מוצגים רק תחת "מתקדם".
נדרשים 2 טיפוסי שדה חדשים ב-`lib/fields.ts`: ‏`select` (אופציות כלליות)
ו-`number` (סליידר עם טווח) — שימושיים לכל המערכת, לא רק לאנימציה.

### 3.3 בלוקים חדשים (מדף → 17)

1. **פס נגלל (marquee)** — טקסטים/מילים שנעים בלולאה אינסופית; מהירות,
   כיוון, נטיית "מדבקה" (rotate -1.4deg), כפתור השהיה מובנה. מבוסס
   ישירות על מתכון ה-RTL מהאתרים הקיימים.
2. **הצהרה (statement)** — סקשן sticky גבוה שבו פסקה גדולה נדלקת
   מילה-מילה לפי התקדמות הגלילה (rAF + ‏`--progress`; עובד גם
   בפיירפוקס). זה בלוק ה"וואו" שמצטלם.

בנוסף, בלוק **שלבי תהליך** הקיים מקבל את אפקט "הקו המצויר" (הקו נמתח
בין השלבים ב-reveal), ובלוק ההמלצות/שירותים מקבל סטאגר ילדים.
**מונה מספרים (count-up)** נכנס כשדרוג לבלוק ההירו (שורת הסטטיסטיקות)
עם `Intl.NumberFormat('he-IL')` ורינדור הערך הסופי ב-SSR.

## 4. שלבי המימוש (סוכני sonnet, סדרתיים)

> לפני כתיבת קוד כל סוכן קורא את `node_modules/next/dist/docs/` הרלוונטי
> (הוראת AGENTS.md) ואת תמציות המחקר.

### שלב א' — המנוע (תשתית, בלי שינוי נראה לעין)
`lib/effects.ts`, הרחבת `lib/theme.ts` + `normalizeTheme`, קלאסי `.fx-*`
ו-CSS של reveal ב-`globals.css` (+ תיקון reduced-motion), סקריפט
`data-js` ב-layout, פרימיטיבים ב-`components/fx/`, עוטף `ThemeScope`
עם `data-fx-*` ו-`ThemeFxLayers`, שדות `select`/`number` ב-fields.
**שער יציאה:** build+lint נקיים; הדפים הקיימים נראים זהים (ערכות ישנות
מנורמלות ל-none/flat).

### שלב ב' — אינטגרציית הבלוקים
עטיפת כל הבלוקים ב-Reveal לפי `_anim` (ברירת מחדל ירושה מהערכה);
כותרת ההירו ב-word-stagger משולב עם הדגשת `**`; סטאגר ילדים בגרידים;
tilt/spotlight/glass לפי `cardStyle`; כפתורי push/shine; beam על מסלול
מומלץ במחירון; parallax ו-exit-fade כהעשרת `view()`; שני הבלוקים
החדשים; count-up בהירו; רישום ב-registry.
**שער יציאה:** build+lint; דף הדמו רץ עם הערכה החדשה בלי שגיאות קונסול.

### שלב ג' — הסטודיו
- טאב עיצוב: סקשנים חדשים — **אנימציה** (צ'יפים: כבוי/עדין/דינמי/פסיכי),
  **רקע** (סוג + עוצמה + גרעון), **כרטיסים**, **זוהר**, **כפתורים**.
- **חבילות פונטים:** 8 צמדים מוכנים ככרטיסים חיים ("נועז", "יוקרתי",
  "אלגנטי", "נקי", "מודרני", "חברותי", "מסורתי", "קמפיין") + הבוררים
  הידניים תחת "התאמה ידנית".
- **עורך בלוק:** קבוצת "אנימציה" גנרית (צ'יפים + "מתקדם" מקופל) לכל בלוק.
- **איפוסים בשלוש רמות:** ‏↺ ליד כל שדה ששונה מברירת המחדל (אינדיקטור
  "שונה" + לחיצה מוחקת את הדריסה); "אפס בלוק" בעורך הבלוק; "אפס ערכה"
  ו"אפס דף" עם אישור + שמירת סנאפשוט אחרון ל"ביטול פעולה אחרונה".
- **הפעל אנימציות ↻** בסרגל התצוגה (key bump) + כיבוי תנועה אוטומטי
  בזמן הקלדה; אנימציה מופעלת מחדש אוטומטית כששדה אנימציה משתנה.
- **🎲 "ערבב לי":** קומבינציה אקראית מאוצרת (חבילת פונטים + פלטה +
  עיגול פינות + עוצמת אנימציה) שנבדקת בניגודיות לפני הצגה; ניתן לגלגל
  שוב ולבטל.
**שער יציאה:** build+lint; כל פקד חדש משנה את התצוגה החיה מיידית.

### שלב ד' — תוכן, ערכה שלישית ותיעוד
ערכת "נובה פסיכית" (אורורה + זוהר + tilt + beam — ההדגמה המקסימלית);
עדכון שתי הערכות הקיימות (מפתחות חדשים, שמרניים); דף הדמו מקבל marquee,
statement וסטטיסטיקות; עדכון README, GUIDE (פרק "אפקטים ואנימציות" +
כללי טעם: רקע אחד לעמוד, אפקט נע אחד-שניים), implementation-notes.

### שלב ה' — QA מקצה לקצה (סוכן ייעודי)
סקריפט Playwright מול build אמיתי שמוודא **כל סעיף בתוכנית**:
- reveal נכנס בגלילה; word-stagger בהירו; marquee נע ונעצר בכפתור;
  statement נדלק בגלילה; count-up סופר; tilt מגיב לסמן; beam מסתובב;
  אורורה מרונדרת; ספוטלייט עוקב.
- **הזזת בלוק משנה את סדר ה-DOM בתצוגה בפועל** (↑↓ בסטודיו ואימות
  סדר הסקשנים) — הדרישה המפורשת.
- איפוס שדה/בלוק/גלובלי מחזירים ערכים; חבילת פונטים מחליפה את שני
  הפונטים; "ערבב לי" משנה ערכה ועובר ניגודיות; replay מריץ מחדש.
- ‏reduced-motion: הדף נטען עם כל התוכן גלוי; ‏JS כבוי: התוכן גלוי.
- אפס שגיאות קונסול; build + lint נקיים; מובייל 390px בלי גלישה רוחבית.
ממצאים → תיקונים → אימות חוזר, עד ירוק מלא.

### שלב ו' — סגירה
שילוב ממצאי 4 סוכני האימות של הסבב הקודם (רצים במקביל לתוכנית זו),
קומיט ודחיפה ל-`claude/page-design-system-febey3`, עדכון המסמכים.

## 5. מה במודע לא בפנים (ולמה)

- **WebGL / three.js** — האורורה ב-CSS מכסה ~80% מהאפקט ב-0KB; ‏alon.ai
  משלם על הסצנה שלו ב-CPU per-frame. שלב עתידי אם יידרש.
- **חטיפת גלילה בהירו** (כמו ה-CinematicHero של shosh) — היסטוריית
  הגרסאות שם מתעדת את מלחמות הקצה; ל-builder זה סיוט תמיכה. scrub
  ב-`view()` נותן את התחושה בלי החטיפה.
- **טיימליין keyframes למשתמש** (מלכודת Webflow) — פריסטים בלבד.
- **hover-preview על צ'יפים** — נדחה לסבב הבא; במקומו החלה מיידית +
  ‏replay אוטומטי (זול ומרגיש חי).
- **אנימציית אותיות בודדות** — סטאגר מילים נותן 90% מהאפקט בלי סיבוכי
  ניקוד; נשקל בעתיד עם `Intl.Segmenter`.

## 6. סיכונים ובלמים

| סיכון | בלם |
|---|---|
| ערכות/דפים ישנים נשברים ממפתחות חדשים | `normalizeTheme()` + ברירות מחדל שמרניות (none/flat) לכל ערכה קיימת |
| תוכן מוסתר אצל משתמשי reduced-motion / בלי JS | עוגת 4 השכבות + בדיקת QA ייעודית לשני המצבים |
| הצפת אנימציה ("יותר מדי") | ברירת מחדל "עדין"; "פסיכי" עם תקרות; כלל רקע-אחד-לעמוד ב-GUIDE |
| ביצועי מובייל | לולאות transform/opacity בלבד; glass מונחת; בדיקת 390px ב-QA |
| טקסט לא קריא על רקע חי | scrim אוטומטי מאחורי טקסט הירו; תקרת עוצמת רקע; פאנל הניגודיות הקיים |
| הידרציה (SSR≠client) בבלוקים עם JS | ערכים סופיים ב-SSR (count-up, statement), אפקטים רק אחרי mount |
