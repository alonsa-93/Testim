# המדריך המלא: בניית דף נחיתה מ-א׳ עד ת׳

זה המדריך המעשי. הוא מניח שהמערכת רצה אצלך (`npm install && npm run dev`)
ושאתה מול `http://localhost:3000/studio`. להסבר על הארכיטקטורה — README.md.

---

## שלב 1 — פותחים דף חדש

בטאב **מבנה ותוכן** לוחצים **דף חדש וריק** (או מתחילים מדף הדוגמה אם
העסק דומה). ממלאים מיד את ״הדף שאני עורך״:

| שדה | מה לכתוב | דוגמה |
|---|---|---|
| שם הדף | שם פנימי שלך | `קליניקת ד״ר לוי — ראשי` |
| מזהה | באנגלית, קובע את הכתובת | `levi-clinic` ← `/p/levi-clinic` |
| כותרת לדפדפן ולגוגל | עד ~60 תווים, עם מילת החיפוש | `ד״ר לוי — רפואת שיניים באשדוד` |
| תיאור לגוגל | עד ~155 תווים, סיבה ללחוץ | `תור תוך 48 שעות, ללא כאבים...` |

הכול נשמר אוטומטית בדפדפן תוך כדי עבודה — סגירה בטעות לא מאבדת כלום.

## שלב 2 — מרכיבים את שלד הדף

לוחצים **+ הוספת בלוק** ובוחרים. זה הסדר שעובד לרוב דפי הנחיתה,
מלמעלה למטה — כל שורה עונה על שאלה שעולה בראש של הגולש:

| # | בלוק | השאלה שהוא עונה עליה |
|---|---|---|
| 1 | סרגל ניווט עליון | איפה אני? (נעוץ אוטומטית לראש הדף) |
| 2 | כותרת ראשית (Hero) | מה מציעים לי ולמה שאתעניין? |
| 3 | פס לוגואים | מי עוד סומך עליהם? |
| 4 | פס נגלל | תזכורת קצרה ונעה — מילות מפתח/יתרונות בלולאה (אופציונלי, קישוט) |
| 5 | שירותים / יתרונות | מה בדיוק אני מקבל? |
| 6 | אודות / שלבי תהליך | איך זה עובד ומי האנשים? |
| 7 | גלריית עבודות / וידאו | תראו לי הוכחות |
| 8 | הצהרה | משפט "וואו" אחד שמתחזק תוך כדי גלילה — נקודת שיא אמצע-דף (אופציונלי) |
| 9 | המלצות לקוחות | מה אומרים לקוחות אמיתיים? |
| 10 | מחירון / מסלולים | כמה זה עולה? |
| 11 | שאלות נפוצות | ומה לגבי ההתלבטות שלי? |
| 12 | טופס לידים | איך משאירים פרטים? |
| 13 | באנר קריאה לפעולה | תזכורת אחרונה לפני הפוטר |
| 14 | כפתור וואטסאפ צף | ערוץ מיידי למי שלא ממלא טפסים |
| 15 | פוטר | פרטים, ניווט, נגישות ופרטיות (נעוץ לתחתית) |

לא חייבים הכול — דף ממוקד עם 6–8 בלוקים מנצח דף עמוס. "פס נגלל" ו"הצהרה"
הם קישוט/אפקט ולא מידע חיוני — מוסיפים רק אם הם מתאימים לאופי העסק.
מסדרים עם ↑↓, מוחקים עם ✕.

## שלב 3 — כותבים את התוכן

לוחצים על בלוק ברשימה ועורכים. כמה כללים שמוכחים בשטח:

- **כותרת ההירו** היא 80% מהדף: תוצאה ללקוח, לא תיאור שלך.
  ❌ ״משרד אדריכלות ותיק״ ✅ ״בית שמרגיש **בדיוק** כמו שדמיינתם״.
  מילה בין `**` `**` נצבעת בצבע המבטא.
- **כפתור ראשי אחד** לכל הדף, תמיד אותה פעולה (״לתיאום ייעוץ״), תמיד
  מקשר ל-`#contact`. הכפתור המשני — למתלבטים (״איך זה עובד״ ← `#about`).
- **מספרים מנצחים תארים**: ״+240 פרויקטים״ חזק מ״ניסיון עשיר״.
- **המלצות** עם שם, יישוב ופרויקט — ככל שספציפי יותר, אמין יותר.
- **קישורי הניווט** הם עוגנים: `#features`, `#pricing`, `#faq`,
  `#contact`. אם מחקת בלוק — הסר גם את הקישור אליו מהניווט ומהפוטר.
- שדה שנשאר ריק פשוט לא מוצג (כפתור משני, ציטוט, תגית) — זו הדרך
  ״לכבות״ חלק מבלוק.

## שלב 4 — מעצבים

עוברים לטאב **עיצוב**:

1. בוחרים **ערכת בסיס** קרובה לאופי העסק (״פרימיום נקי״ לעסקים
   יוקרתיים/מקצועיים, ״נובה כהה״ לטק/דיגיטל, ״נובה פסיכית״ רק כהשראה
   קיצונית — ראו סעיף אפקטים למטה, לא ערכת ברירת מחדל ללקוח רגיל).
2. **צבעים**: primary = צבע המותג של הלקוח; accent = צבע הדגשה משלים;
   השאר לרוב לא צריך לזוז.
3. **פאנל הנגישות** חייב להיות ירוק כולו לפני מסירה — אם צירוף נכשל,
   כפתור **תקן** בוחר אוטומטית צבע טקסט תקין.
4. **פונטים**: השורה המהירה היא **חבילת פונטים** — 8 צמדים מוכנים ככרטיס
   חי (נועז/יוקרתי/אלגנטי/נקי/מודרני/חברותי/מסורתי/קמפיין), כל אחד עם
   כותרת+גוף שכבר עובד יחד. בוחרים אחד ומתקדמים. רק אם צריך שליטה נפרדת
   בכותרת ובגוף — פותחים "התאמה ידנית": כותרות סריפיות (Frank Ruhl Libre)
   = יוקרתי; Heebo/Rubik = מודרני; Varela Round = ידידותי; טקסט רץ —
   Assistant או Heebo כמעט תמיד.
5. **אפקטים ואנימציה** (סקשנים: אנימציה / רקע / כרטיסים / זוהר / כפתורים):
   - עוצמת אנימציה: **עדין** היא ברירת המחדל הבטוחה לכל לקוח עסקי;
     **דינמי** לדף עם קצת יותר אנרגיה (השקה, מוצר דיגיטלי); **פסיכי**
     כמעט אף פעם לא ללקוח אמיתי — זו עוצמת ה"מקסימום" להדגמה/קמפיין
     חד-פעמי בלבד.
   - **אורורה/רעש (noise)**: מדליקים כשרוצים תחושת "פרימיום חי" —
     סטארטאפ, מוצר טכנולוגי, קמפיין. לא לעסק שירותי מסורתי (עורך דין,
     קליניקה) — שם רקע `none` נראה רציני יותר.
   - **זוהר (glow)**: `soft` לכרטיס-CTA מודגש בעדינות; `intense` רק
     יחד עם רקע כהה ואנרגטי — על רקע בהיר הוא נראה "מלוכלך", לא זוהר.
   - זוכרים את שני הכללים מ-README: **רקע אחד לעמוד**, **אפקט נע
     אחד-שניים בלבד** (beam/שימר/ברק — לא בכל כרטיס).
   - **🎲 ערבב לי**: הדרך המהירה לגלות קומבינציה — מגלגל פונטים+פלטה+
     פינות+עוצמה יחד, תמיד עובר ניגודיות (אין תוצאה שנכשלת ב-AA).
     אפשר לגלגל שוב או לבטל לקומבינציה הקודמת.
   - **הפעל אנימציות ↻** בסרגל התצוגה מריץ מחדש את כל ה-reveal — שימושי
     אחרי שינוי עוצמה, בלי לרענן את הדף.
6. בודקים ב**נייד** (הכפתור למעלה) — רוב הגולשים יגיעו משם.

נותנים לערכה שם ומזהה (למשל `levi-clinic`) — כך היא נשמרת כערכה של
הלקוח הזה. שינוי ששונה מברירת המחדל מסומן ב-↺ ליד השדה — לחיצה מוחקת
את הדריסה וחוזרת לערך שהערכה נולדה איתו. יש גם "אפס ערכה" ו"אפס דף"
ברמת הכל-או-כלום (עם אישור ו"ביטול פעולה אחרונה"), למקרה שהתעסקות
השתבשה ורוצים להתחיל מחדש.

## שלב 5 — מפרסמים

1. **הורדת הדף** ← לשמור את `levi-clinic.json` בתיקיית `pages-data/`.
2. **הורדת הערכה** ← לשמור את `levi-clinic.json` (של הערכה) בתיקיית `themes/`.
3. שתי שורות בכל אחד משני קובצי הרישום:

```ts
// themes/index.ts
import leviClinic from "./levi-clinic.json";
export const themes: Theme[] = [..., leviClinic as Theme];

// pages-data/index.ts
import leviClinicPage from "./levi-clinic.json";
export const pages: Page[] = [..., leviClinicPage as Page];
```

4. `npm run build` לוודא שהכול ירוק — והדף חי ב-`/p/levi-clinic`.
5. `git add -A && git commit && git push` — הדף מגובה ומוכן לפריסה.

## עדכון דף קיים

הקובץ בריפו הוא האמת. פותחים את הסטודיו ← **ייבוא** ← בוחרים את קובץ
הדף מ-`pages-data/` ← עורכים ← **הורדת הדף** ← דורסים את הקובץ הישן.
(הייבוא מזהה לבד אם הקובץ הוא דף או ערכה.)

## צ׳ק-ליסט לפני מסירה ללקוח

- [ ] פאנל הנגישות כולו ירוק (AA לפחות)
- [ ] נבדק במצב נייד — אין גלישה לרוחב, התפריט נפתח
- [ ] כל קישורי הניווט והפוטר מצביעים לבלוקים שקיימים בדף
- [ ] טלפון, וואטסאפ ואימייל אמיתיים של הלקוח (הירו, טופס, פוטר, וואטסאפ צף)
- [ ] כותרת ותיאור לגוגל מולאו
- [ ] אין טקסט דוגמה שנשכח (״סטודיו אלון״, ״050-000-0000״)
- [ ] הצהרת נגישות ומדיניות פרטיות הושלמו (התבניות ב-`app/accessibility`
      ו-`app/privacy` מסומנות ⚠️ — חובה להשלים לפני אוויר)
- [ ] טופס הלידים חובר ליעד אמיתי (כרגע מדומה — מסומן בקוד)

## תקלות נפוצות

| תופעה | סיבה ופתרון |
|---|---|
| הדף לא מופיע ב-`/p/...` | הקובץ לא נרשם ב-`pages-data/index.ts`, או שה-build לא רץ מחדש |
| ״סוג הבלוק אינו מוכר״ | קובץ דף ישן עם type שאין ב-registry — בדקו את `components/blocks/registry.ts` |
| ערכה לא מופיעה בסטודיו | לא נרשמה ב-`themes/index.ts` |
| הסטודיו נפתח עם דף ישן | זו הטיוטה השמורה — ״טעינת דף הדוגמה״ או ייבוא הקובץ הרצוי |
| פונט לא נטען | אין רשת ל-Google Fonts — הדף עובר אוטומטית לפונט מערכת |
| וידאו לא מוצג | ודאו קישור רגיל של YouTube/Vimeo — ההמרה להטמעה אוטומטית |

## מה עוד אין (ולאן ממשיכים)

- **תמונות בהירו/אודות** — כרגע קומפוזיציות צבע; בגלריה כבר אפשר להדביק
  כתובת תמונה.
- **חיבור טופס אמיתי** — הנקודה מסומנת ב-`components/blocks/lead-form.tsx`.
- **פריסה ל-Vercel** — `npm run build` כבר ירוק; החיבור הוא צעד אחד.
- **שמירה בענן במקום קבצים** — רלוונטי כשיהיו עשרות לקוחות.
