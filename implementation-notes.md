# יומן מימוש — מערכת העיצוב

מסמך עבודה פנימי (לפי מתודולוגיית unknowns-discovery): סטיות מהתוכנית
המאושרת ויומן הנחות. מתעדכן תוך כדי פיתוח.

## החלטות שאושרו על ידי המשתמש (15.08.2026)

- סטאק: React / Next.js (נבחר: Next.js 16 + Tailwind v4 + TypeScript)
- סוג המערכת: שילוב מלא — ספריית רכיבים + ערכות מתחלפות + סטודיו ויזואלי
- משתמשים: המפתח + לקוחות לא-טכניים (ולכן סטודיו עם תצוגה חיה)
- מיתוג: ערכה נפרדת לכל לקוח
- כיוון עיצובי לערכת ברירת המחדל: נקי ופרימיום

## סבב 2 — בנאי דפים (15.08.2026)

המשתמש הבהיר שהציפייה הייתה מערכת שגם **מייצרת** דפים, לא רק מסגננת
אותם. זה פער בשאלות הפתיחה שלי: כל האפשרויות שהצעתי עסקו בעיצוב וסגנון,
ולא שאלתי אם נדרשת גם בניית דפים. הוחלט בסבב שאלות נוסף:

- אחסון הדפים: קבצי JSON בפרויקט (לא מסד נתונים) — פשטות וורסיוניות בגיט
- עורך התוכן: המפתח בלבד; הלקוחות מקבלים דף מוכן
- עדיפות: בורר בלוקים + עריכת טקסט (ולא הרחבת מדף הבלוקים)

מה נבנה בעקבות זאת: כל הבלוקים הומרו לתוכן מבוסס-props עם סכמת שדות,
נוסף מודל דף (`lib/page.ts`) ומנוע רינדור, ובסטודיו נוסף טאב ״מבנה ותוכן״
עם הוספה, סידור, מחיקה ועריכת כל שדה. נוסף גם בלוק ״שלבי תהליך״.

## סטיות מהתוכנית

| חומרה | סטייה | נימוק |
|---|---|---|
| קלה | הפונטים נטענים בקישור Google Fonts אחד ב-layout במקום next/font | מאפשר החלפת פונט חיה בסטודיו בלי rebuild, ונכשל ברכות (fallback לפונט מערכת) אם אין רשת בזמן build |
| קלה | נוספו דפי placeholder ל"הצהרת נגישות" ו"מדיניות פרטיות" | הפוטר מקשר אליהם; מסומנים בבירור כתבנית שאסור לפרסם כמו שהיא |
| בינונית | הניווט והפוטר נעוצים לראש הדף ולתחתיתו ואינם ניתנים לסידור מחדש | מבנה HTML תקין: `header` לפני `main` ו-`footer` אחריו. מסומן בבירור בפאנל |
| קלה | תיקיית הדפים נקראת `pages-data/` ולא `pages/` | ב-Next.js השם `pages/` שמור לראוטר הישן ועלול להתנגש |

## יומן הנחות (Assumption Ledger)

| הנחה | למה נדרשה | סיכון אם שגויה | איך מאמתים |
|---|---|---|---|
| עברית RTL היא ברירת המחדל; LTR יתווסף בעתיד | המשתמש כותב עברית ובונה לשוק הישראלי | נמוך — הרכיבים בנויים על logical properties ויעבדו גם ב-LTR | לשאול כשיגיע לקוח באנגלית |
| ערכות נשמרות כקובצי JSON בריפו, לא במסד נתונים | פשטות, ורסיוניות בגיט, אפס תלות בתשתית | בינוני — עם עשרות לקוחות זה יציק; המעבר ל-DB לא ישבור את הרכיבים | כשמספר הלקוחות יעבור ~10 |
| עריכת תוכן (טקסטים/תמונות) מחוץ לתחום בשלב זה | המשתמש ביקש מערכת עיצוב וסגנון | נמוך — תוכן נערך בקוד; אפשר להוסיף שכבת תוכן בהמשך | פידבק מהמשתמש אחרי שימוש ראשון |
| טופס הלידים לא מחובר לשום שירות | אין עדיין CRM/יעד אמיתי; חיבור מסומן בקוד | נמוך — מסומן בהערה בולטת בקוד וב-README | לפני עליית דף אמיתי לאוויר |
| התוכן בדף הדוגמה פיקטיבי ("סטודיו אלון") | נדרש תוכן דמו ריאליסטי להדגמה | אין — מוחלף בכל פרויקט אמיתי | — |
| בלוק שנוסף לדף נכנס עם תוכן הדוגמה שלו ולא ריק | קל יותר לערוך טקסט קיים מאשר למלא דף ריק | נמוך — יש כפתור איפוס, והטקסט גלוי לעריכה מיד | פידבק אחרי בניית דף אמיתי ראשון |
| תוכן שלא נשמר בקובץ הדף נלקח מברירות המחדל של הבלוק | קובצי דף קצרים וקריאים; שינוי ברירת מחדל משתקף בכל הדפים | בינוני — שינוי ברירת מחדל של בלוק ישנה דפים קיימים שלא דרסו אותה | לייצא דף מלא מהסטודיו כשרוצים לקבע תוכן |

## מה נבדק לפני המסירה (15.08.2026)

- [x] `npm run build` עובר נקי (9 דפים סטטיים)
- [x] `npm run lint` עובר נקי
- [x] `/`, `/studio`, `/preview/default`, `/preview/nova-dark` נטענים ואומתו בצילומי מסך (דסקטופ + מובייל)
- [x] החלפת ערכה חיה בדף הבית אומתה בדפדפן (רקע הדף התחלף לערכת nova-dark)
- [x] פאנל הקונטרסט אומת בדפדפן: צבע primary בהיר הקפיץ כפתור "תקן", והתיקון האוטומטי בחר טקסט כהה תקין
- [x] צירופי הצבעים בשתי הערכות המובנות עוברים AA

## סבב 3 — סבב אימות רב-סוכני ושיפורים (15.08.2026)

הופעל צוות של 6 סוכני אימות במקביל. שני סוכני ה-E2E השלימו 60+ בדיקות
(כולן עברו מלבד הממצאים למטה); 4 סוכנים (ביקורת קוד, נגישות/RTL סטטית,
E2E ציבורי, ביצוע README) נעצרו במגבלת שימוש — **מומלץ להריץ אותם בסבב
עתידי** (הפרומפטים שמורים בסקריפט ה-workflow).

ממצאים שאושרו ותוקנו:

| חומרה | ממצא | תיקון |
|---|---|---|
| גבוהה | ייבוא דף עם בלוקים ללא id: עריכה זלגה בין בלוקים ומחיקה מחקה את כולם | `normalizePage()` משלים מזהים חסרים/כפולים ו-meta, רץ בייבוא ובטעינת טיוטה |
| גבוהה | פונט הגוף וגודל הבסיס לא הוחלו בתצוגת הסטודיו וב-preview (ירשו מה-body) | קלאס `.ds-scope` על כל עוטף ערכה מחיל רקע/צבע/פונט/גודל מהמשתנים המקומיים |
| נמוכה | שני מופעים של אותו בלוק יצרו id כפול ב-DOM | ה-PageRenderer מעניק עוגן קנוני למופע הראשון וסיומת רצה לבאים (#features-2) |
| נמוכה | הודעת סטטוס שנייה נמחקה מוקדם על-ידי טיימר ישן | clearTimeout לפני כל flash |
| נמוכה | הסמן קפץ לסוף שדה מזהה בזמן הקלדה | סניטציה עברה ל-blur ולייצוא |
| נמוכה | ערך hex פגום נשמר ויוצא בלי חיווי | ולידציה חיה בשדה הצבע: מסגרת אדומה, שמירה רק של ערך תקין, חזרה לערך האחרון ב-blur |

נוספו לפי בחירת המשתמש: 4 בלוקים (וואטסאפ צף, וידאו מוטמע, פס לוגואים,
גלריית עבודות — תמונות מ-URL עם fallback דקורטיבי), GUIDE.md — מדריך
בנייה מלא בעברית, ולוגואים+וואטסאפ נוספו לדף הדמו. תמונות בשאר הבלוקים,
חיבור הטופס ופריסה ל-Vercel — נדחו במפורש על-ידי המשתמש לסבב עתידי.

אימות סבב 3 (דפדפן אמיתי): נרמול ייבוא ואי-זליגת עריכה, עוגנים ייחודיים
(features/features-2), ירושת פונט וגודל בסיס בתצוגה (17px→20px על פסקה
רגילה), hex פגום מסומן ולא נשמר, וואטסאפ נשאר בגבולות תצוגת הסטודיו
(טריק translateZ), אפס שגיאות קונסול. build+lint נקיים.

## סבב 4 — אימות מלא (14 סוכנים) + שלב א' של מנוע האפקטים (15.08.2026)

הופעלו מחדש 4 הסוכנים שנעצרו בסבב 3 (ביקורת קוד, משמעת עיצוב-קוד, E2E
ציבורי, ביצוע README) יחד עם שני סבבי E2E שכבר עברו — סה"כ 14/14 סוכנים
הושלמו בהצלחה, 0 שגיאות. `code-correctness` ו-`e2e-public` (סבב קודם)
עברו נקי לגמרי. `code-design-discipline` ו-`docs-walkthrough` (חדשים
הפעם) העלו 6 ממצאים אמיתיים, כולם אומתו נגד הקוד בפועל:

| חומרה | ממצא | טיפול |
|---|---|---|
| גבוהה | צבע שגיאה קבוע (#E5484D) עוקף את מנוע הטוקנים ונכשל ב-WCAG AA (3.59–3.84:1, נדרש 4.5) בשתי הערכות; בלתי נראה לפאנל הניגודיות | נוסף `colors.danger` לכל ערכה (מחושב ל-≥4.5:1 מול bg ו-surface), `--ds-color-danger`, `text-danger`/`border-danger` ב-Tailwind, ושתי שורות חדשות בפאנל הניגודיות שבודקות אותו |
| גבוהה | שדה חובה בטופס הליד לא מתוקשר לקוראי מסך (אין aria-required) | `Field` משכפל את הילד עם `aria-required` כשה-prop `required` מופעל |
| גבוהה | border כמעט בלתי נבדל מ-bg בשתי הערכות (1.07–1.53:1, נדרש 3:1 לפי 1.4.11) | border הועמק בערכת default (#8F8977, 3.43:1) ובוהר בנובה כהה (#5A72B8, 3.53:1); נוספה שורת בדיקה בפאנל |
| גבוהה (לא שוחזר) | חשד לחפיפה בין ThemeSwitcher לכפתור וואטסאפ לקישורי הפוטר בגלילה לתחתית | נבדק בדפדפן אמיתי (דסקטופ 1280×800 ומובייל 390×844, `/p/demo`): המרווח האמיתי 20–100px, אין חפיפה. הסוכן עצמו ציין שלא הצליח לאמת ויזואלית — ממצא שקרי-חיובי |
| נמוכה | צבע וואטסאפ קבוע (#25D366) מחוץ למנוע הטוקנים | לא תוקן — חריגה מתועדת ומכוונת בקוד (מותג חיצוני קבוע) |
| נמוכה | טאבי הסטודיו (aria-pressed) לא תבנית ARIA Tabs מלאה; אין robots.txt/sitemap/OG | לא תוקן בסבב זה — לא חוסם, נשאר לרשימת המשך |
| גבוהה | README: דוגמת "הוספת בלוק חדש" משתמשת ב-gallery שכבר קיים וכבר רשום | הדוגמה הוחלפה בבלוק "team" שלא קיים, כך שהמתכון ניתן לביצוע מילולי |
| בינונית | README: "קובץ אחד + שורה אחת" לא מדויק — נדרש גם import וגם הוספה למערך; גם ב-themes/index.ts | נוסח תוקן ל"קובץ אחד + שתי שורות", שני הסניפטים מראים import מפורש |

**הערת סביבה (לא קוד-בפועל, לא תוקן):** סוכן ה-docs-walkthrough שחזר
במדויק כשל FATAL של Turbopack ("Symlink ... points out of the
filesystem root") כש-`npm run build` רץ בתוך git worktree עם
`node_modules` מקושר-symlink לריפו הראשי — מצב שנוצר אוטומטית על ידי
סוכני workflow מבודדים. עוקף עם `next build --webpack`. לא רלוונטי
לריפו הראשי (node_modules כאן הוא תיקייה רגילה, לא symlink) — מתועד
כאן ליתר ביטחון לסוכן workflow עתידי שירוץ בתוך worktree.

**שלב א' של מנוע האפקטים** (לפי WORKPLAN.md, קודם למחקר של 4 סוכנים על
אנימציות/3D/UX-של-בילדרים/ניתוח shosh-marketing ו-ALON.AI): נבנתה
התשתית — `lib/effects.ts`, הרחבת `Theme.effects` + `normalizeTheme`,
מערכת ה-reveal וכל קלאסי ה-`.fx-*` ב-`globals.css`, פרימיטיבים תחת
`components/fx/` (Reveal, SplitWords, TiltCard, SpotlightGroup, CountUp,
ThemeFxLayers), ו-`ThemeScope` שמחליף את כל עוטפי `.ds-scope` הישנים.
build+lint נקיים; אומת שהדפים הקיימים מרונדרים זהה למצב הקודם (אפס
שינוי חזותי בשלב זה, כמתוכנן).

## אימות סבב 2 — בנאי הדפים (נבדק בדפדפן אמיתי)

- [x] עריכת הכותרת הראשית מתעדכנת בתצוגה החיה, כולל הדגשת מילה ב-`** **`
      שקיבלה את צבע המבטא של הערכה (rgb(180,138,74) = ‎#B48A4A)
- [x] הוספת בלוק ״שלבי תהליך״ מבורר הבלוקים — הבלוק נוסף ורונדר בדף
- [x] עריכת שדה בתוך רשימה (שם השלב הראשון) התעדכנה בתצוגה
- [x] מחיקת בלוק (המלצות) הסירה אותו מהדף
- [x] אפס שגיאות JavaScript בקונסול לאורך כל התרחיש
- [x] `npm run build` (10 דפים) ו-`npm run lint` נקיים
